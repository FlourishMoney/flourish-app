# Copy to .env and fill in. .env is gitignored and is never printed by the pipeline.
# Without these the pipeline still runs end to end using the macOS "say" voice.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
