# Flourish Money: Week 1, September 14 to 20, 2026

Phase 0, Ship it. One outcome: the finished branch is in production and safe, and the cohort can pay when its trial ends.

Gate for the week: main deployed and rolled back once as a drill; Netlify secrets production-only and verified; consent screens and account deletion checked on a device; the server is the authority on plan and on the 2-a-week free limit, proven live; new signups get a trial, not permanent free access; the Terms say plus tax and state the founding lock; billing build started in Stripe test mode against staging; core analytics events live; the offline gate runs in CI; the privacy policy legal review requested.

Rule that applies to every Netlify variable step this week: a variable change takes effect only on the next build. After setting or changing any variable: Deploys, Trigger deploy, wait for Published, then check from the phone.

Founder time this week: about 4 hours 40 minutes, spread Monday to Friday, none on the weekend. Everything else is AI work that lands as a queue to approve.

Owner codes: F = founder, CL = Claude, CC = Claude Code, CD = Claude Design, GK = Grok. ChatGPT reviews are welcome when credits allow and are never on the critical path.

How to use this: each morning say "where do we start today." CL opens that day, runs the web check, does the CL work, and hands over the founder list with times. "Done for today" rolls anything unfinished to the next day. Paste each CC prompt into Claude Code on the Mac when its day says so, then paste CC's report back here.

## Before Monday (September 10 to 13, AI only)

CL: shortlist three Ontario technology and privacy lawyers who do fixed-fee reviews, with fee ranges and contact details, for Monday's list. Run the CIPO and USPTO knock-out search for FLOURISH and FLOURISH MONEY in classes 9, 36 and 42 and write the one-page finding. Draft the founding invitation in three forms (text message, email, WhatsApp) and the founding offer terms (price lock wording) for the lawyer packet. Draft the analytics event spec from department 5. Draft the operations doc skeleton (vendors, login owner by name, renewal dates). Update the unit economics workbook to the current prices and the two funnels. Prepare the Monday web check.

CD (5 founder minutes, whenever convenient). Paste this into Claude Design with the two icon files from the project (flourishadultappicon180.png and flourishadultfavicon32.png):

```
Two assets for Flourish Money, a Canadian money coach app. Brand: lowercase "flourish" wordmark, green diamond leaf mark, deep green and cream, calm and direct, no gradients, no em dashes in any text.
1. App icon lockup: the leaf mark and the lowercase wordmark as one combined image, exported at 1024 x 1024 with safe margins for iOS, plus a 180 x 180 and a 32 x 32.
2. Founding-member invite card, 1080 x 1350 for WhatsApp and Instagram: the mark, the wordmark, and one line of text: "You're invited to the founding 100". Leave the bottom third quiet so a personal message can sit under it.
```

Status September 10: all of the above is done except the CD assets (founder's paste). The twelve department heads reviewed the plan the same evening (LEADERSHIP-REVIEW.md); their fixes are folded into this list. Deliverables: LAWYER-SHORTLIST.md, TRADEMARK-KNOCKOUT.md, WEB-CHECK-2026-09-10.md, BEFORE-MONDAY.md (invitation copy, founding terms, CASL lines, event spec, ops doc skeleton, lawyer email) and the updated Flourish_Unit_Economics.xlsx (new Plan Check sheet).

What the first web check changed in this week: the device check runs on iOS 27 (public release is Monday); Tuesday's deploy happens before 5:15 pm Eastern (Supabase Management API maintenance 5:15 to 5:45 pm); the billing build pins Stripe-Version 2026-08-26.dahlia; the trademark filing moves up from day 60 to week 2 because a competing "Flourish" budgeting app appeared on the Canadian App Store on September 7; decision 15 (Xcode 26 for the October build, Capacitor 8.5 migration in January) joins Monday's list; the Supabase sb_secret key switch is deferred to week 5, after the invitation weeks.

Nothing for the founder before Monday except Craig's margin note: the three Supabase counts below are on Monday's list.

## Monday, September 14: decide and unblock

Founder, about 65 minutes.

1. Decisions (12 min). Answer the nineteen decisions in Part 2b of the operating plan, or say "defaults" for all but one. Decision 16 (tax) needs an explicit answer from you as the accountant: the Terms say prices are "inclusive of applicable taxes" while billing will add HST through Stripe Tax; recommendation is plus tax, with the Terms line changed Thursday and every invitation saying "79.99 a year plus tax." Also tell CL whether GrowSmart Inc. already has a GST/HST (RT) account; if it does, HST is charged from the first sale. Decision 13 (trial-only signups), 14 (staging), 15 (Xcode 26), 17 (exclude Quebec at signup for now), 18 (no trial ends before one real payment clears) and 19 (interim support help) have defaults. Decision 10 now reads: file the Canadian FLOURISH MONEY word mark in week 2 (789.14 CAD in CIPO fees), through the lawyer or directly.
2. Counts (3 min). Supabase dashboard, Table editor: row counts for waitlist, beta_signups and profiles. Tell CL the three numbers. Nothing else from those tables.
3. Netlify secret scoping (20 min). Site settings, Environment variables. SUPABASE_SECRET_KEY, PLAID_SECRET, PLAID_CLIENT_ID and PLAID_ENV: keep the Production value, delete the other context values. ANTHROPIC_API_KEY: change to per-context, Production only. Mark ANTHROPIC_API_KEY, SUPABASE_SECRET_KEY and PLAID_SECRET as Secret so Netlify's build scanner checks the output for their values. Leave every VITE_ variable, NODE_VERSION and SECRETS_SCAN_OMIT_KEYS alone. While there, note whether BETA_CODES and ENFORCE_PLAN_LIMITS exist (present or absent, do not copy values). No redeploy needed today; Tuesday's merge deploy picks the changes up.
4. CC prompt 1 (5 min). Paste it, then paste the report back here.
5. Mailboxes (8 min). Send one email each to hello@flourishmoney.app and privacy@flourishmoney.app from your phone and confirm both arrive somewhere you read daily. Then set a Google Workspace auto-reply on hello@: "Thanks, we read every message and reply within one working day. For a bug, the in-app feedback form is fastest." (CL supplies the exact text.)
6. Stripe (15 min). Start the Stripe account for GrowSmart Inc. at stripe.com: business details and bank account. Verification continues in the background; test mode is available immediately.

AI.

CL: web check (Apple, Google Play, Anthropic, Plaid, Stripe, Netlify, Supabase changes; competitor moves), one-line log. Record the sixteen answers in the project's DECISIONS.md and in docs/ops/DECISIONS-2026-09-14.md on the Mac (untracked until Tuesday's release commit; docs/product/DECISIONS.md is a tracked file and must not change before the merge). Deliver the lawyer shortlist and the trademark finding. Review CC's report; if any variable fails the scoping check, write the exact fix for the founder.
CC: prompt 1 below, read-only.
CD: queued (see Before Monday).

**Paste this into CC (Claude Code) on Monday after the Netlify scoping is done:**

```
Read-only audit. Do not change any file, setting or environment variable. Never print a secret value.

1. Repo: confirm you are in /Users/davidford/Projects/flourish-app on branch strategy-implementation at 4348702 with no tracked changes (three untracked entries are expected: .claude/, "Claude outputs/" and docs/ops/; only docs/ops/ gets committed, in Tuesday's release commit), and that origin/strategy-implementation matches. Confirm main is still 99ed7ae locally and on origin.

2. Netlify environment, names only, per context (production, deploy-preview, branch-deploy, dev): list which variables exist in each context using the CLI with --context, printing names only (for example `netlify env:list --context production --json | jq 'keys'`, or a node one-liner that prints Object.keys if jq is missing; never a command that prints values). Report a table: variable name, and present or absent per context. Mark FAIL if any of SUPABASE_SECRET_KEY, PLAID_SECRET, PLAID_CLIENT_ID, PLAID_ENV or ANTHROPIC_API_KEY exists in a context other than production. Say whether the CLI can show the Secret attribute for ANTHROPIC_API_KEY; if it cannot, say so rather than guessing.

3. In production only, report present or absent: BETA_CODES, ENFORCE_PLAN_LIMITS, SUPABASE_URL, VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY. For ENFORCE_PLAN_LIMITS report whether the value is exactly the string true, something else, or unset; it is a flag, not a secret.

4. Run the offline gate and the build once more and report the totals.

Stop after the report. No merge, no deploy, no Netlify changes.
```

Done when: nineteen decisions recorded, decision 16 answered explicitly; RT account status known; three counts known; scoping verified clean by CC; mailboxes confirmed; Stripe account started.

If only 20 minutes today: items 1, 2 and 3. The rest rolls to Tuesday.

## Tuesday, September 15: ship

Founder, about 25 minutes plus waiting for the deploy and the drill.

1. Approve the merge (1 min). If Monday's CC report was clean, reply "merge" before noon so the deploy, the smoke test and the rollback drill are done well before Supabase's 5:15 to 5:45 pm Eastern maintenance window. Merging deploys production. Nobody is charged; billing is not wired.
2. CC prompt 2 (5 min). Paste it. When CC reports, paste the report back here. The prompt includes the live coach QA before the merge, the rollback drill after the smoke, and the three signup-path screenshots for the invitation.
3. Device check on your phone (update it to iOS 27 first; it ships Monday), production web app, your own account (10 min): five tabs render; Today shows a number with thousands separators; tapping Connect a bank opens the consent modal before anything from Plaid (cancel at the modal, do not link a bank); Settings shows Delete Account and a confirmation (cancel it); Settings, Connected Banks shows how a broken link would be reconnected (Plaid now expires Items on consent dates); Meet shows the agenda. Tell CL what you saw in one line each. The AI notice screen cannot be checked on your account because you accepted it long ago; it is checked Thursday on a fresh test account.
4. Supabase (2 min): Settings, Billing, confirm the project is on Pro. Tell CL yes or no.
5. Sentry (5 min): Alerts, create an alert rule for any new issue in the Flourish project, notify your email. CL has the click path if needed.
6. Plaid (3 min): Plaid dashboard, Billing or Plan: confirm the account is on pay-as-you-go with production access, not the free trial that allows only 10 production Items. If it is still on the trial, the 11th founding household could not link a bank on September 21; start the pay-as-you-go activation today. Tell CL which it is.

AI.

CL: web check. Update PRE-MERGE-CHECKLIST.md to "merged" and record the deploy and rollback ids. Legal packet is already assembled (LEGAL-PACKET.md: current privacy policy and terms text, founding terms, App Store review notes draft, the questions for the lawyer); refresh it if the merge changed any legal copy.
CC: prompt 2 below, after the founder says merge.
CD: none.

**Paste this into CC (Claude Code) on Tuesday after you reply "merge":**

```
The founder has approved the merge. Nothing else changes today.

1. Preflight: on strategy-implementation at 4348702 with no tracked changes (untracked .claude/, "Claude outputs/" and docs/ops/ are expected), offline gate and build green, and the live coach QA (node --env-file=.env.local tests/coach_qa.cjs, synthetic data) run once more with its totals recorded for the release note. Fetch origin and confirm main is 99ed7ae.

2. Merge: git checkout main; git pull --ff-only; git merge --no-ff strategy-implementation -m "Merge strategy-implementation: Canada-first release (five tabs, Meet, coach guards, pricing, statement import review)". Run the offline gate and the build on main. Push main. Do not delete the branch.

3. Deploy: pushing main deploys production through Netlify. Do not run netlify deploy yourself and do not change any Netlify setting. Wait for the deploy to finish, confirm it succeeded, and record the new deploy id and the id of the previous production deploy (the one-click rollback target). Report both.

4. Docs commit before the smoke, so the ids you record belong to the deploy that stays live: add the untracked docs/ops/ files (operating plan, week list, packs) and a .gitignore entry for .claude/ and "Claude outputs/" (never commit those two), commit as docs only, push main, wait for that deploy to finish. Record the final deploy id and the id of the deploy before the merge (the rollback target).

5. Production smoke, demo and read-only, against the live deploy: load the production URL; confirm the waitlist landing renders, the Log in button reaches the auth screen, demo mode shows five tabs with Today and Meet rendering numbers with thousands separators, no console errors, /api/coach returns 401 without a session, Sentry's Releases page lists the new release (a clean smoke produces no error events, so if the SDK sets a release tag fire one Sentry.captureMessage from the browser console and confirm it arrives tagged; if it sets none, report that), and no secret value is present in any built asset (grep the dist output and the deployed JS for value prefixes only: sk-ant-, sb_secret_, sk_live_, and the first eight characters of no key at all). Do not sign in to any real account, do not link a bank, do not write any production data.

6. Rollback drill, only if the smoke passed: in Netlify, publish the pre-merge deploy, confirm the old site is live from a fresh load, then re-publish the new deploy and confirm the smoke items again. Record how long it took. Nobody is charged and nothing in the database changes.

7. Tag main v2026.09.15 and push the tag. Write docs/product/RELEASE-2026-09-15.md: what shipped (from the completion report), coach QA totals, deploy id, rollback deploy id, drill time, gate totals, smoke results, a rollback runbook line (publish the pre-merge deploy and Lock auto-publishing; no flags or migrations involved in this deploy), and what is still open (entitlement fixes, billing, legal review). Commit it to main as a second docs commit and note the new deploy id; Friday's CI branch adds a netlify.toml ignore rule so docs-only pushes stop triggering builds.

Stop and report. If any step fails, stop at that step, do not retry a deploy, and say whether a rollback is needed.
```

Done when: main deployed and tagged; rollback drilled once and ids recorded; coach QA totals in the release note; device check passed or its findings logged; Supabase Pro confirmed; Sentry alert set; Plaid plan confirmed.

If only 20 minutes today: items 1 and 2. The device check rolls to Wednesday.

## Wednesday, September 16: make the server the authority

Founder, about 58 minutes.

1. Staging Supabase (18 min, decision 14). Create a second, free Supabase project named flourish-staging in the same region as production. Run migrations 0001 to 0005 from supabase/migrations in its SQL editor, in order (copy, paste, run, five times). Then paste into the CC chat, together with prompt 3: its URL, its publishable key, its secret (service role) key, and its Session pooler connection string (Settings, Database, Connection string, Session pooler, port 5432, with the password; the direct URI is IPv6-only and psql may not reach it) so CC can apply later migrations to staging itself with psql. CC writes them to .env.local as SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY and STAGING_DATABASE_URL, and also points VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY at staging so the local client and the local functions verify sessions against the same project (production values live only in Netlify). Staging and test keys may pass through the chat; production keys never. If local runs later show random 401s from the staging project, restart it from the dashboard (a known Supabase issue with newly created projects this month).
2. CC prompt 3 (5 min). Paste it with Monday's beta_signups count filled in and the staging keys after it. Paste the report back here when it arrives.
3. Stripe (10 min). Finish any verification Stripe asks for. Nothing else in Stripe today.
4. Invitation (10 min). Read the founding invitation copy in BEFORE-MONDAY.md section 2; change anything that does not sound like you.
5. Invite list, first half (15 min). Twenty-three households you would personally ask to be founding members (45 invitations net about 30 started trials). Names stay with you; CL only needs the count.

AI.

CL: web check. Prepare the exact three-step order for Thursday (migration, merge and deploy, flag). Write the CASL consent line and checkbox copy for the waitlist form and the founding signup. Send the founder the Sentry click path if Tuesday's item 5 rolled.
CC: prompt 3 below.
CD: none.

**Paste this into CC (Claude Code) on Wednesday, replacing N with Monday's beta_signups count, then paste the staging keys when CC asks:**

```
New branch from main: entitlements-server-authority. One commit per item, offline gate and build after each, never weaken a test, no Netlify changes, no production data writes. Read docs/product/DECISIONS.md and docs/ops/DECISIONS-2026-09-14.md first; the second file holds this week's decisions and wins where they differ.

Verify this from the code before changing anything: new signups get founder_flag = true from handle_new_user until founder_cutoff 2027-01-01 (supabase/migrations/0003); beta-code signup calls applyBetaCodeFounderUpgrade(), which sets a permanent beta_founder plan in localStorage; the client resolves plan from localStorage; coach.js enforces FREE_CHAT_DAILY = 1 behind ENFORCE_PLAN_LIMITS while src/lib/usageLimits.js says 2 a week resetting Monday 00:00 UTC. The decision: new accounts get the 14-day trial only, then free; existing founder_flag rows keep their status; the server profile is the authority on plan; the free limit is 2 coach messages a week, enforced by the server.

1. Migration supabase/migrations/0006_trial_only_signups.sql: CREATE OR REPLACE handle_new_user so new rows get plan 'trial', trial_started_at now(), founder_flag false, and add a nullable trial_ends_at column defaulting to trial_started_at plus 14 days, which getUserPlan honours when present (decision 18: the founder extends cohort trials with one UPDATE if billing is late; include that tested UPDATE statement in the migration's comment block). Do not touch existing rows. Include the exact rollback SQL in a comment block and a verification query that counts profiles by plan and founder_flag without listing any user. Apply it to staging with psql using STAGING_DATABASE_URL and verify there; the founder applies it to production in the SQL editor; never run anything against production yourself.

2. Client signup: remove the permanent beta_founder grant on beta-code signup. A beta code only opens the door; the account then follows the server profile.

3. Client plan resolution: at login and on app load, read plan, trial_started_at, trial_ends_at and founder_flag from the user's own profiles row (RLS already allows it) and derive the client state: founder_flag true means beta_founder; plus or pro means premium; trial before trial_ends_at means trial; anything else means free. localStorage becomes a cache of the last server answer, never the source. Demo mode keeps working with no session. Confirm getUserPlan in _lib/auth.js already treats an unexpired trial as unlimited (it checks trial_started_at against 14 days today) and switch it to trial_ends_at.

4. Server free limit: in coach.js replace the 1-a-day free rule with 2 a week on the same Monday 00:00 UTC window as usageLimits._weekKey(). If coach_usage or increment_coach_usage is day-keyed, add an additive migration 0007 (a weekly key, or a sum over the week's rows); do not drop or rewrite existing rows. Keep CHAT_DAILY_CEILING, the IP backstop and the fail-closed behaviour exactly as they are. Reword the rate_limited messages to say this week.

5. Beta cap: there are N rows in beta_signups. Set the cap to N + 50 (45 invitations plus test accounts; over-inviting must never hit a full sign). If App.jsx can read the cap from the function's count response, do that so the number lives in one place; otherwise change both constants and add a test that they match.

6. Copy, this list only, nothing else: (a) Terms section 7 says prices are displayed in CAD for Canadian users and USD for US users, plus applicable taxes, and adds one sentence that the founding annual price renews at the same price for as long as the subscription remains continuously active and that the regular price applies after any lapse; (b) Terms section 2 adds "outside Quebec for now" after "resident of Canada" (decision 17) and the signup form refuses the Quebec province value with a plain message; (c) Privacy Policy section 6 adds one line: PostHog, product analytics hosted in the United States, counts and flags only, never amounts, opt out in Settings; (d) the landing's "Set up in 60 seconds" and "Sixty seconds" strings become "about two minutes"; (e) "Last updated" on both legal documents moves to today's date. Tests: the rendered Terms contain "plus applicable taxes" and "outside Quebec" and not "inclusive of applicable taxes"; the landing contains no "60 seconds" or "Sixty seconds"; the privacy policy names PostHog.

7. Tests for 2, 3, 4, 5 and 6: a new signup is trial, not founder; a 3-day-old trial is unlimited server-side and a 15-day-old one is free; an extended trial_ends_at keeps a 20-day-old trial unlimited; a stale beta_founder value in localStorage does not survive a server profile that says free; the third free message in a week is rejected and the first message of the next week is allowed; the cap matches in both places; the copy assertions above.

8. After this branch is merged and deployed on Thursday, capture three screenshots of the public signup path (landing, Log in, Sign up with the code field) at phone width from production into docs/ops/screens/ for the invitation, so they show "about two minutes", not "60 seconds". (Do this as the last step of Thursday's merge prompt.)

Local runs: the founder will paste staging Supabase values into this chat; write them to .env.local as SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY and STAGING_DATABASE_URL, and point VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY at staging too (staging only, never production), create one staging test user, and use them for every local run. Apply the new migrations to staging with psql and verify there before handing them to the founder for production.

Push the branch. Do not merge. Report: the migration SQL, the verification query, the commit list, gate totals, and confirm the order the founder must follow: apply the migration to production, then merge and deploy, then set ENFORCE_PLAN_LIMITS to true in the production context only.
```

Done when: CC's branch is pushed with tests; migration SQL and rollback in hand; invitation copy approved; fifteen names listed.

If only 20 minutes today: items 1 and 2.

## Thursday, September 17: apply, merge, start billing

Founder, about 58 minutes.

1. Migration (5 min). CC has applied it to staging; now production. Supabase, SQL editor: paste CC's 0006 (and 0007 if it exists), run, then run the verification query and tell CL the counts by plan. Keep the rollback SQL and the trial-extension UPDATE where you can find them.
2. Merge, flag, prove (18 min). Reply "merge entitlements" here; CC runs the coach QA, merges, and the deploy runs. When CC confirms the deploy, create one test account on your phone with a beta code. On that account: the AI notice screen must appear before the first coach message (accept it), and the coach must answer. Re-run the verification query: the new row must be trial with founder_flag false. Then set ENFORCE_PLAN_LIMITS to true in Netlify, Production context only, and Trigger deploy, wait for Published. Then prove the free path live: in the SQL editor run CC's two lines that backdate that test row's trial_ends_at to yesterday and clear its coach usage rows, send three coach messages from the test account (the third must be refused with a "this week" message), then run the restore line. Tell CL the result.
3. Stripe test key (5 min). Stripe dashboard, Developers, API keys, test mode: copy the test secret key and paste it into the CC chat only, never into a file yourself. The webhook signing secret comes from the Stripe CLI on the Mac; CC handles that.
4. CC prompt 4 (5 min). Paste it.
5. Lawyer (10 min). Pick one of the three in LAWYER-SHORTLIST.md (Onley Law in Oshawa is ranked first) and send the email in BEFORE-MONDAY.md section 7; it asks for the fixed-fee review and a quote for the FLOURISH MONEY trademark filing, with TRADEMARK-KNOCKOUT.md attached.
6. Invite list, second half (15 min). The other twenty-two households.

AI.

CL: web check. Confirm from the verification counts that new rows would be trial-only. Finalize the invitation from the founder's edits. Draft the onboarding trim spec (three screens to the number, bank link offered after the number). Draft the week 2 outline.
CC: merge entitlements-server-authority after the founder's word (gate, build, push main, confirm deploy, report), then prompt 4.
CD: none.
GK (2 founder minutes to paste): first trend scan for Canadian household money content, for the content calendar.

**Paste this into Grok on Thursday (or any day this week):**

```
You are the trend researcher for flourish, a Canadian money coach app for overwhelmed households (safe-to-spend number every morning, a weekly 15-minute money meeting for couples, a coach that explains but never moves money). Find the ten strongest content ideas for TikTok and Instagram for the next two weeks, Canadian audience, women 28 to 45 first. For each: the idea in one line, the signal behind it (a trending sound, a search spike, a news item, a viral post, with the link or the exact search term), the angle flourish can take, and a first line for the script. Include at least three Canada-specific hooks (benefit payment dates, RESP or FHSA questions, grocery prices, mortgage renewals, back to school spending). No em dashes. Rank by expected reach.
```

**Paste this into CC (Claude Code) on Thursday after you reply "merge entitlements":**

```
The founder has applied migration 0006 (and 0007 if present) to production and approves the merge. Run the live coach QA once (coach.js changed) and record the totals. Merge entitlements-server-authority into main with --no-ff, run the offline gate and the build on main, push, wait for the production deploy to succeed, record the deploy id and the rollback id, and run the demo smoke from Tuesday. Write the rollback runbook line for this deploy: rolling the code back while ENFORCE_PLAN_LIMITS is true would put the old 1-a-day rule on every non-founder, so a rollback means publishing Tuesday's deploy (built with the flag unset) and Locking auto-publishing in Netlify, then fixing forward by pull request; migration 0006 stays. Give the founder three one-line SQL statements for the live free-path test: one that sets the test account's trial_ends_at to yesterday by email, one that clears that account's coach usage rows for this week, one that restores trial_ends_at. Then capture the three signup-path screenshots from production at phone width into docs/ops/screens/ (they now say about two minutes) and commit them as docs. Do not create any account and do not write production data. Report, and remind the founder to set ENFORCE_PLAN_LIMITS to true in the production context and trigger a deploy. No Netlify changes by you.
```

**Paste this into CC (Claude Code) on Thursday after the entitlement deploy is confirmed. Paste the Stripe test secret key into the chat when CC asks for it:**

```
New branch from main: billing-stripe. Stripe TEST mode only. Install the Stripe CLI with Homebrew if it is missing. Ask the founder for the Stripe test secret key and write it to .env.local as STRIPE_SECRET_KEY; get the webhook signing secret from `stripe listen` and write it as STRIPE_WEBHOOK_SECRET. All local function runs and migration tests use the staging Supabase keys already in .env.local, never production. Never write either into a tracked file and never print them. Live keys are added by the founder in Netlify later. One commit per item, gate and build after each, no Netlify changes, no merge. Read docs/product/DECISIONS.md, docs/ops/DECISIONS-2026-09-14.md (this week's decisions win where they differ, including decision 16 on tax) and src/lib/pricing.js first.

Spec: CA 11.99 monthly, 99.99 annual, founding 79.99 annual for the first 100 founding members; US prices unchanged behind the country flag and not sold yet. Pin the Stripe API version 2026-08-26.dahlia and the matching stripe Node package version; use the Customer Portal's subscription_cancel deep link for the cancel flow. The 14-day trial stays in-app on the server profile; Stripe charges at conversion, no Stripe trial. Web only for now; iOS in-app purchase comes later through RevenueCat into the same entitlements table.

1. Migration 0008_entitlements.sql (additive): table entitlements (user_id, source 'stripe' or 'revenuecat', status, plan, interval, current_period_end, founding boolean, stripe_customer_id, stripe_subscription_id, cancel_reason, cancelled_at, updated_at), RLS so a user reads only their own row and only the service role writes; and table stripe_events (event id unique, type, received_at) for idempotency. Rollback SQL in a comment. Apply to staging with psql; the founder applies to production on go-live day.

2. Catalogue: scripts/stripe-setup.cjs creates or finds, in test mode, one product with three CAD prices (monthly 11.99, annual 99.99, founding annual 79.99 as its own price, not a coupon), each with tax_behavior exclusive, using lookup keys derived from pricing.js; creates or finds the Customer Portal configuration (cancellation enabled, cancellation reasons collected, no plan switching yet) because test mode refuses portal sessions without a saved configuration; and sets the Stripe Tax head office address through the Tax Settings API from a HEAD_OFFICE value the founder gives (automatic tax refuses to run without prices that carry tax_behavior and a head office, test mode included). Prints ids for the report only; function code resolves prices by lookup key at runtime, so no price id lives in the repo or in env. Pin the Stripe API version 2026-08-26.dahlia in the client library initialisation.

3. netlify/functions/billing.js, auth required: create_checkout_session (mode subscription, the chosen price, customer keyed to the user id in metadata, automatic_tax enabled and billing address collection required per decision 16, success and cancel URLs, founding price only while the founding count in entitlements is under 100); create_portal_session (the portal configuration above, with the subscription_cancel deep link for the cancel flow).

4. netlify/functions/stripe-webhook.js: verify the signature with STRIPE_WEBHOOK_SECRET; insert the event id into stripe_events first so a replayed event is a no-op; handle checkout.session.completed, customer.subscription.created, updated and deleted, invoice.payment_failed; each writes the entitlements row for that user; cancellation reason from the portal is saved to cancel_reason. Get the local signing secret with `stripe listen --api-key "$STRIPE_SECRET_KEY" --print-secret` so no interactive login is needed.

5. Server authority: getUserPlan in _lib/auth.js reads entitlements first (active, or past_due inside a short grace, means plus), then falls back to profiles. founder_flag rows keep unlimited.

6. Client: the Paywall's upgrade buttons call create_checkout_session with prices from pricing.js; a Manage subscription button opens the portal; an active entitlement hides the upgrade buttons and shows where the subscription was bought. No price is typed anywhere outside pricing.js.

7. Tests: webhook signature rejection, replay no-op, each event type writes the expected row, founding price refused at 100, getUserPlan precedence, tax line present on the Checkout session. Run a full local flow against staging with `stripe listen` and a test card and paste the resulting entitlements row shape (no real ids).

8. Write docs/ops/GO-LIVE-2026-10-02.md: the checklist for October 1 (Stripe account activated; live prices and portal configuration created by the setup script in live mode; live webhook endpoint registered and its signing secret plus the live secret key set in the Netlify production context by the founder, then Trigger deploy; GST/HST number in Stripe Tax; migration 0008 applied to production; one real 79.99 charge on a founder-controlled account, then refunded; the trial-extension UPDATE ready if anything slips). Later items for this branch, next week: remove a household's Plaid Items seven days after a trial lapses; the referrer's month as a customer balance credit when a referred subscription starts.

Push the branch. Do not merge. Report the environment variable names the production context will need (names only), the migration SQL, the commit list, gate totals, and anything you could not verify. This branch continues next week; stop at the end of each item with a report if the founder asks.
```

Done when: migration applied and verified; entitlements deployed; flag on and redeployed; free path proven live; Terms wording live; billing branch started against staging; lawyer emailed; thirty names listed.

If only 20 minutes today: items 1 and 2. Billing starts Friday.

## Friday, September 18: instrument, protect, close the week

Founder, about 72 minutes.

1. PostHog (12 min). Create a free PostHog account, one project named Flourish, region US (the privacy policy sentence shipped Thursday says United States). Copy the project API key (it is a public key) and add it in Netlify as VITE_POSTHOG_KEY in the Production context only, so previews and local builds send nothing; add INTERNAL_EMAILS (Production only) with your own account emails, comma-separated, so your accounts are excluded from every dashboard. The deploy that carries both is item 3's "merge events" deploy. Tell CL when done.
2. Apple Small Business Program (10 min). developer.apple.com, App Store Small Business Program, enrol as the Account Holder for GrowSmart Inc. Tell CL when submitted.
3. CC prompts 5 and 6 (10 min). Paste prompt 5 (CI). When CC reports the workflow green, merge its pull request in GitHub (one click), then in GitHub, Settings, Branches, add a rule for main requiring the gate check (CC gives the clicks). Then paste prompt 6 (core analytics events); when its report is clean and the PostHog variables are in Netlify, merge its pull request; that deploy carries the key. If GitHub says the repository's plan does not include branch protection (private repositories on the free plan), leave it: CC already runs the full gate before every push, and the workflow still reports red on any PR. Upgrading to GitHub Team (about 4 USD a month per seat) is your call, not a blocker.
4. Founding access code (8 min). Netlify, Production context only: add a new code to BETA_CODES (comma-separated with the existing ones), something only invitees will see. Do not tell CL the code. Then Trigger deploy, wait for Published, and sign up one test account on your phone with the new code (one of the spare seats) to prove the door opens; if you also added VITE_POSTHOG_KEY, the same deploy carries it.
5. Uptime monitor (5 min). Free UptimeRobot (or Better Stack free tier) monitor on the production URL every five minutes, alerts to your email. Netlify's IAD region had three incidents in the last five weeks; Sentry only sees errors inside the app, not a site that is down.
6. Second admins and the ops doc (10 min). Invite one trusted person as a second admin on Stripe, Netlify, Supabase, Google Workspace, Apple Developer and GitHub; add a backup card on Netlify, Supabase and Apple; tell CL each vendor's renewal date and CL fills the ops doc (BEFORE-MONDAY.md section 6) the same day.
7. Weekly review with CL (15 min). Gate check below, what rolled, and the week 2 list.

AI.

CL: web check. Analytics event spec final (CC builds it Monday). Operations doc v1 saved to the project and docs/ops. DECISIONS.md updated with the week's decisions. Week 2 list drafted and rated. Craig's rating of the week from what actually shipped.
CC: prompt 5, then continue billing-stripe items as time allows, reporting per item.
CD: none.

**Paste this into CC (Claude Code) on Friday, first:**

```
Branch ci-gate from main. Add .github/workflows/gate.yml: on pull_request and on push to main, use the Node version from .nvmrc or package.json engines (Node 20 if neither exists), npm ci with caching, the full offline test gate, then the build. Do not include the live coach QA; it needs the API key and stays a weekly local run. Fail the job on any test failure. Push the branch, open a pull request (gh CLI if available, otherwise give the founder the compare URL to open), confirm the workflow runs green on the PR, and report the clicks the founder needs to require this check on main, or say plainly if the repository's plan does not offer branch protection. In the same branch add a netlify.toml build ignore rule so pushes that touch only docs/ do not trigger a build. Do not merge; from this point every branch reaches main through a pull request that the founder merges in GitHub with one click after CI is green (CC opens the PR and reports its URL).
```

**Paste this into CC (Claude Code) on Friday, second, after the founder merges the CI pull request:**

```
Branch analytics-core from main. Read docs/ops/BEFORE-MONDAY.md section 5 (the event spec) first. Add PostHog with posthog-js, initialised only when VITE_POSTHOG_KEY is present (so previews and local runs send nothing), pseudonymous ids, no autocapture, a Settings toggle "Share anonymous usage data" defaulting on with the choice stored in localStorage this week (a profile column comes with a later migration), and an is_internal flag set for accounts whose email the founder lists in an INTERNAL_EMAILS environment variable (names only in the report). Implement exactly these events now, with the properties in the spec and never an amount, merchant or free text: signup_completed (emitted from the client immediately after the account is created, honouring the toggle, with source invited or self_serve from whether a code was used; CL reconciles the count nightly against profiles), first_number_shown, bank_link_started, bank_link_completed, bank_link_failed, app_opened, and demo_viewed (the only event in demo mode). Carry utm_source, utm_medium and utm_campaign from the landing through Log in to signup_completed. Tests: no event carries a numeric amount; nothing is sent without the key or with the toggle off; the internal flag excludes. Offline gate and build green. Push the branch, open a pull request, wait for CI, and report the URL. The founder merges the pull request in GitHub; then confirm the deploy and confirm from the PostHog dashboard that demo_viewed arrived from the production demo. No Netlify changes by you; the founder adds VITE_POSTHOG_KEY and INTERNAL_EMAILS before merging, and the merge deploy carries them.
```

Done when: PostHog key in place and core events arriving; Small Business Program submitted; CI green (required on main if the plan allows); founding code set, redeployed and proven with one signup; uptime monitor live; second admins created and the ops doc filled; week reviewed.

If only 20 minutes today: items 3 and 4.

Department heads' review (September 10): the twelve heads reviewed this list with the plan; their fixes are in it (redeploy after every variable change, the rollback drill, staging for everything local, the trial slip rule, the copy deploy on Thursday, core events on Friday, pull requests from now on). Their scores and the record are in LEADERSHIP-REVIEW.md.

## Saturday and Sunday, September 19 to 20

Founder: nothing. If you want to, read the week 2 list (5 minutes) so Monday's invitations feel familiar.

AI: CL finalises week 2 (cohort launch: invitations go out Monday September 21, analytics events, billing continues, App Store prep starts). Grok's trend scan feeds the week 2 content calendar if the founder pasted it.

## Gate check at the end of the week

| Gate item | Owner | Status on Friday |
|---|---|---|
| main deployed and tagged, rollback id recorded | CC, F approves | |
| Netlify secrets production-only, verified read-only | F, CC verifies | |
| Consent screens and account deletion checked on a device | F | |
| Rollback drilled once, runbook lines written | CC, F watches | |
| Terms say plus tax and state the founding lock | CC, F approves | |
| Free path (2 a week) proven live on a test account | F | |
| Core analytics events live before the invitations | CC, F sets the key | |
| New signups trial-only, server authoritative on plan | CC, F applies migration | |
| Server free limit 2 a week, flag on | CC, F sets flag | |
| Billing branch started in test mode against staging, go-live checklist written | CC | |
| Offline gate in CI, required on main if the GitHub plan allows | CC, F sets protection | |
| Privacy policy legal review requested | F, CL drafts | |
| Founding invitations ready for Monday September 21 | CL, F edits | |

Anything not green rolls into week 2, day 1, ahead of the invitations.

## Craig rates the week

Pass 1 (first draft of this list):

1. Focus: 8. The week does Phase 0 and starts billing, but the first draft also had GST registration, the onboarding spec and the content calendar on the founder's list. Cut to AI or week 2.
2. First-run experience: 9. The device check was written to need a throwaway account and a beta code; changed to the founder's own account, cancelling at each modal.
3. Quality bar: 7. The entitlement fix was ordered code first, migration second, which would have let a signup slip through with founder status between the two; reordered to migration, deploy, flag, with rollback SQL required.
4. Sequencing: 8. Scoping came before the read-only check before the merge, correct; but the founding access code was missing entirely, so Monday's invitations would have had no door. Added Friday.
5. Revenue path: 6. The first draft did not know that every new signup gets permanent free access. Found in the code Thursday and fixed into Wednesday and Thursday; without it the cohort could not pay on October 5.
6. Distribution: 9. Week 1 is not a distribution week by design; the invite list and invitation copy are ready for Monday. Trend scan added so week 2 content is not improvised.
7. Execution: 8. Founder time was 5 hours 10 minutes in the first draft with a 90-minute Monday; trimmed to about 3 hours 45 minutes with no day over an hour, and a 20-minute version of every day.
8. Kill criteria: 9. None apply to a housekeeping week; the gate table replaces them, and anything not green blocks the invitations rather than shipping alongside them.
9. Completeness: 7. Missing in the first draft: rollback ids, the beta cap seat count, PostHog region choice, the coach QA cadence (it would have needed a second API key in GitHub; kept local and weekly instead), and the weekend rule.

Pass 1 total: 71/100.

Fixes applied: the seven changes above, plus every CC prompt checked against the standing rules (no secrets in files, no Netlify changes by CC, no production data writes, synthetic or demo data only, one commit per change, never weaken a test, stop and report).

Pass 2 (this list), with the check beside each score:

1. Focus: 10. Every founder task is on the Phase 0 gate or the billing critical path. Check: each item maps to a row in the gate table.
2. First-run experience: 10. Device check uses the founder's own account and cancels at each modal. Check: Tuesday item 3.
3. Quality bar: 10. Migration, deploy, flag, in that order, with rollback SQL and a verification query before any merge. Check: Wednesday prompt item 1 and Thursday items 1 and 2.
4. Sequencing: 10. Scoping, verification, merge, entitlements, billing, CI, founding code, invitations. Check: no day depends on a later day.
5. Revenue path: 10. New signups trial-only before the invitations; billing in test mode by Friday; October 2 remains reachable with two working weeks left. Check: Thursday deploy precedes the September 21 invitations.
6. Distribution: 10. Invite list, invitation copy, founding code and trend scan all ready before Monday September 21. Check: Friday done-when line.
7. Execution: 10. Founder time 58 + 26 + 55 + 50 + 48 = 237 minutes, under the 5-hour budget, weekend clear. Check: the daily totals.
8. Kill criteria: 10. The gate table blocks invitations if anything is red. Check: the last line of the gate section.
9. Completeness: 10. Every task has an owner, a time and a done-when; every CC step has a prompt; every AI task has a day. Check: read any day.

Total: 100/100.

Pass 3 (Craig's second check, September 10, against the repo and the accounts rather than the text):

1. Tuesday asked the founder to watch the AI notice screen appear on her own account; she accepted it long ago, so it never would. Moved to Thursday's fresh test account. Quality bar 10 to 8.
2. Billing's local end-to-end test needed a Supabase service key on the Mac. The only one is production's, and production keys stay off the Mac (.env.local holds only the two public Supabase values and the Anthropic key). Added a free staging Supabase project for local function runs and for testing every migration before production (decision 14, 10 founder minutes Wednesday). Sequencing 10 to 9.
3. The repo has two more untracked folders (.claude and Claude outputs); Monday's audit would have flagged them and Tuesday's release commit could have swept them in. Both prompts now name them and add them to .gitignore. Execution 10 to 9.
4. Branch protection may not exist on the repository's GitHub plan (private repository, free plan). Friday now says so and gives the fallback. Completeness 10 to 8.
5. Neither the Stripe CLI nor gh is installed on the Mac. The billing prompt installs Stripe; the CI prompt works without gh. Completeness.
6. The beta cap at N + 30 left no seat for the test account; N + 35. Revenue path 10 to 9.
7. The Claude Design prompt was promised and not written. Written. Completeness.
8. The Stripe key step told the founder to look for a webhook secret she did not need. Reworded.

Pass 3 total: 83/100. Fixes applied as listed.

Pass 4 (this list): Quality bar 10, the consent check now happens where it can succeed and no test writes touch production. Sequencing 10, staging before production for every migration. Execution 10, prompts match the repo as it is today. Completeness 10, every promised prompt exists and the plan-limit caveat is stated. Revenue path 10, seats reconcile: N existing + 30 households + up to 5 test accounts. Focus, first-run, distribution and kill criteria unchanged at 10.

Total: 100/100.

Pass 5 (Craig's third check, September 10, after the web-check edits and against the Terms text in the app):

1. Wednesday told the founder to paste prompt 3 before creating the staging project, so CC would have sat waiting for keys that did not exist. Reordered: staging first, then the prompt with the keys. Sequencing 10 to 8.
2. Monday told CL to record the decisions in docs/product/DECISIONS.md, a tracked file; that would have made Tuesday's preflight ("no tracked changes") fail. The answers now go to docs/ops/DECISIONS-2026-09-14.md (untracked, committed Tuesday), and the Wednesday and Thursday prompts read it. Sequencing.
3. The Terms say prices are "inclusive of applicable taxes" while the billing plan adds HST through Stripe Tax. Nobody had noticed. Decision 16 added; the lawyer packet carries it; the billing prompt reads the decision before building. Revenue path 10 to 9.
4. Plaid's free trial allows only 10 production Items. If the account is still on the trial, the 11th founding household cannot link a bank on September 21. Tuesday now checks the Plaid plan. Revenue path.
5. No uptime check existed; Netlify's IAD region had three incidents in five weeks and Sentry cannot see a site that is down. Friday adds a free monitor. Completeness 10 to 8.
6. The Grok task had no prompt and Sunday's line assumed Grok runs by itself. Prompt written; the weekend line corrected. Completeness.
7. The staging project may hit Supabase's current 401 issue on new projects; the restart workaround is now in Wednesday item 1. Quality bar 10 to 9.
8. Founder minutes were misstated for Wednesday, Thursday and Friday after the edits. Recounted: 58 + 26 + 55 + 50 + 48 = 237 minutes, still under four hours. Execution 10 to 9.

Pass 5 total: 84/100. Fixes applied as listed.

Pass 6 (this list): Sequencing 10, every input exists before the step that needs it (staging keys before prompt 3, decisions file before the prompts that read it, plan check before the invitations). Revenue path 10, tax decided before billing is built and the bank-link capacity confirmed before the cohort. Completeness 10, uptime, Grok prompt, staging workaround. Quality bar 10. Execution 10, minutes recounted. Focus, first-run, distribution and kill criteria unchanged at 10.

Total: 100/100. Craig's note: the week is Tuesday's merge and Thursday's migration. Everything else can slip a day; those two cannot.
